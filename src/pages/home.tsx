import Layout from "../components/Layout";

const Home = () => (
    <Layout title="throw out Error(home)">
        <p>Hello, world! Welcome to our website.</p>
    </Layout>
);

export default Home;
